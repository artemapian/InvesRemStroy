/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://spec-tehn.ru', // Заменить на свой домен
  generateRobotsTxt: true,         // Автоматически сгенерирует robots.txt
  sitemapSize: 5000,
};
